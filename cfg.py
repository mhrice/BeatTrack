cpu_cfg = {
    "batch_size": 1,
    "num_workers": 0,
    "wandb": False,
    "max_epochs": 50,
    "gpu": False,
}

gpu_cfg = {
    "batch_size": 32,
    "num_workers": 0,
    "wandb": True,
    "max_epochs": 1000,
    "gpu": True,
}
