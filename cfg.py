cpu_cfg = {
    "batch_size": 1,
    "num_workers": 0,
    "wandb": False,
    "max_epochs": 50,
}

gpu_cfg = {
    "batch_size": 96,
    "num_workers": 10,
    "wandb": True,
    "max_epochs": 1000,
}
